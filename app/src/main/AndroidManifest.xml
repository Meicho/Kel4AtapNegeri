<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
        <uses-permission android:name="android.permission.RECORD_AUDIO" />

        <application
            android:allowBackup="true"
            android:icon="@drawable/logoatapnegeri"
            android:label="@string/app_name"
            android:roundIcon="@drawable/logoatapnegeri"
            android:supportsRtl="true"
            android:theme="@style/Theme.AtapNegeri"
            android:usesCleartextTraffic="true"
            tools:targetApi="q">

            <activity android:name=".activities.PeralatanActivity"/>
            <activity android:name=".activities.DetailTips"/>
            <activity android:name=".activities.DetailPeralatan" />
            <activity android:name=".activities.DetailGunung" />
            <activity android:name=".activities.ListGunung" />
            <activity android:name=".activities.MainActivity"/>
            <activity android:name=".activities.LoginActivity"/>
            <activity android:name=".activities.RegisterActivity"/>
            <activity android:name=".activities.HomeActivity"/>
            <activity android:name=".activities.LoadActivity"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="YOUR API KEY" />
        </application>

    </manifest>
